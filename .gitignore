__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
dist/
build/
.coverage
.env
.venv/
node_modules/

# Raw sources, page images, OCR intermediates, and generated build
# artefacts live under $TEI_MAKER_DATA — never inside the repo.
# See docs/DATA_LAYOUT.md.
# Defensive gitignores so accidental re-creation in the repo root is silent:
ocr/
images/
